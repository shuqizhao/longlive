<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper  
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="com.example.mapper.UserMapper">  
  
    <select id="selectUsersByCondition" resultType="com.example.dto.user.User" parameterType="com.example.dto.user.User">  
        SELECT * FROM customer where name like concat('%',#{name},'%')
    </select>  
  
    <select id="selectUserById" resultType="com.example.dto.user.User">  
        SELECT * FROM user WHERE id = #{id}  
    </select>  
  
    <insert id="insertUser" parameterType="com.example.dto.user.User">  
        INSERT INTO user (name, email) VALUES (#{name}, #{email})  
    </insert>  
  
    <update id="updateUser" parameterType="com.example.dto.user.User">  
        UPDATE user SET name = #{name}, email = #{email} WHERE id = #{id}  
    </update>  
  
    <delete id="deleteUser" parameterType="int">  
        DELETE FROM user WHERE id = #{id}  
    </delete>  
  
</mapper>